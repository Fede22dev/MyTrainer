<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.app.mytrainer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTrainer">
        <activity android:name=".ui.ActivityCalendar"/>
        <activity android:name=".ui.activities.registration.ActivityRegistrationTrainer" />
        <activity android:name=".ui.activities.registration.ActivityUserChoice" />
        <activity android:name=".ui.activities.registration.ActivityRegistrationAthlete" />

        <activity android:name=".ui.activities.ActivityLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>